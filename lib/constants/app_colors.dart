import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF007BFF); // Electric Blue
  static const Color secondary = Color(0xFFFFFFFF); // White (Monochrome Accent)
  static const Color background = Color(0xFF000000); // Pitch Black
  static const Color surface = Color(
    0xFF0F172A,
  ); // Dark Slate Blue for cards (fallback)
  static const Color card = Color(
    0xFF1E293B,
  ); // Slightly lighter for distinctive cards
  static const Color textPrimary = Color(0xFFFFFFFF); // White
  static const Color textSecondary = Color(
    0xB3FFFFFF,
  ); // White with 70% opacity
  static const Color textTertiary = Color(0x66FFFFFF); // White with 40% opacity
  static const Color accent = Color(0xFF00E5FF); // Cyan Accent
}
